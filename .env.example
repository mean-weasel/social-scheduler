# Supabase Configuration
# Copy this file to .env.local and fill in your values

# For local development, run `supabase start` and get values from `supabase status`
# For production, get these from your Supabase project dashboard

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key - server-side only (for MCP server and admin operations)
# WARNING: Never expose this in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
